let book={
    title:"title leni book",
    author:"Chaithragna",
    pages:100
};
function detailsBook(book){
    console.log(`title of the book is ${book.title}`);
    console.log(`author of the book is  ${book.author}`);
    console.log(`pages of the book is ${book.pages}`);
}
detailsBook(book);